import colors from 'windicss/colors';

export default {
  attributify: {
    prefix: 'w:',
  },
  theme: {
    extend: {
      colors: {
        primary: 'var(--color-primary)',
        secondary: 'var(--secondary-primary)',
      },
    },
  },
};
