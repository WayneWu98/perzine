use axum::{
    routing::{get, post},
    Router,
};

use crate::core::response::HandlerResult;

// async fn get_posts() -> HandlerResult<>

// pub fn get_router() -> Router {
//     Router::new()
//         .route("/", post(login))
//         .route("/:id", get(protected))
// }
